name: texto
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  google_mlkit_text_recognition: ^0.11.0
  permission_handler: ^11.2.0


  cupertino_icons: ^1.0.2
  camera: ^0.10.5+9
  flutter_svg: ^2.0.9
  google_fonts: ^6.1.0
  flutter_spinkit: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  #flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
  assets:
      - assets/camera_broken.svg    
      - assets/history.svg
      - assets/maintenace.svg

